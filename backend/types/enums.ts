
export enum UserRole {
    ADMIN = 'ADMIN_ROLE',
    USER = 'USER_ROLE',
    MEDICO = 'MEDICO_ROLE'
  }
  
  export enum UserState {
    ACTIVE = 'activo',
    INACTIVE = 'inactivo',
    SUSPENDED = 'suspendido'
  }